{
    "name": "api-specs-viewer",
    "version": "1.0.0",
    "description": "OpenAPI specifications viewer",
    "scripts": {
        "build": "node build.js",
        "dev": "npm run build && npx http-server dist -p 8080",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "glob": "^10.3.10",
        "fs-extra": "^11.1.1",
        "http-server": "^14.1.1"
    }
}